- op: add
  path: /spec/template/spec/initContainers/0
  value:
    name: secret-init
    command: [ 'cd', 'admin', 'redis-initial-password' ]
    image: quay.io/argoproj/cd:latest
    imagePullPolicy: IfNotPresent
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
      readOnlyRootFilesystem: true
      runAsNonRoot: true
      seccompProfile:
        type: RuntimeDefault