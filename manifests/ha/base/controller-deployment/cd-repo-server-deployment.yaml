apiVersion: apps/v1
kind: Deployment
metadata:
  name: cd-repo-server
spec:
  replicas: 2
  template:
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                app.kubernetes.io/name: cd-repo-server
            topologyKey: kubernetes.io/hostname
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app.kubernetes.io/name: cd-repo-server
              topologyKey: topology.kubernetes.io/zone
      containers:
      - name: cd-repo-server
        args:
        - /usr/local/bin/cd-repo-server
