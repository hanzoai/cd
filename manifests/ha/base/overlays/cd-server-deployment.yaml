apiVersion: apps/v1
kind: Deployment
metadata:
  name: cd-server
spec:
  replicas: 2
  template:
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                app.kubernetes.io/name: cd-server
            topologyKey: kubernetes.io/hostname
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app.kubernetes.io/name: cd-server
              topologyKey: topology.kubernetes.io/zone
      containers:
      - name: cd-server
        env:
        - name: CD_API_SERVER_REPLICAS
          value: '2'
        args:
        - /usr/local/bin/cd-server
