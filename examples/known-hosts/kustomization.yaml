apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- github.com/hanzoai/cd//manifests/cluster-install?ref=stable
- cd-known-hosts.yaml

patches:
- path: cd-known-hosts-mounts.yaml
