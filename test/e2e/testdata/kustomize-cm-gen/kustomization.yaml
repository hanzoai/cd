configMapGenerator:
  - name: my-map
    literals:
      - foo=bar
generatorOptions:
  annotations:
    apps.hanzo.ai/compare-options: IgnoreExtraneous
    apps.hanzo.ai/sync-options: Prune=false
kind: Kustomization
