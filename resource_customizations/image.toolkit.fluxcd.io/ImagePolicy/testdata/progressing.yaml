apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: podinfo
  namespace: cd
spec:
  imageRepositoryRef:
    name: podinfo
  policy:
    semver:
      range: x.x.x
status:
  conditions: []
